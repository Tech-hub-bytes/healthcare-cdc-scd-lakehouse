# Healthcare CDC → Bronze → Silver (SCD) → Gold lakehouse
bundle:
  name: healthcare_cdc_scd
  uuid: 86b10814-a3b5-45c1-8a1c-876e1487f066

include:
  - resources/*.yml

variables:
  catalog:
    description: Unity Catalog catalog
  schema:
    description: Unity Catalog schema for bronze/silver/gold + CDC landing volume

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-7c3eed4c-66bb.cloud.databricks.com
    variables:
      catalog: workspace
      schema: hc_cdc_lakehouse
  prod:
    mode: production
    workspace:
      host: https://dbc-7c3eed4c-66bb.cloud.databricks.com
      root_path: /Workspace/Users/muhammadaziz977@yahoo.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: hc_cdc_lakehouse
    permissions:
      - user_name: muhammadaziz977@yahoo.com
        level: CAN_MANAGE
